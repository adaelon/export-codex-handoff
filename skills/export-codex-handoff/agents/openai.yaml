interface:
  display_name: "Export Codex Handoff"
  short_description: "Compress a Codex thread in a dedicated task"
  default_prompt: "Use $export-codex-handoff in a fresh dedicated task to compress Codex session <UUID> into a portable Handoff."
